---
---

<section class="hero">
    <p class="eyebrow">Hi, I'm Maddie 🌿</p>
    <h1>I build cozy, human-made websites.</h1>
    <p class="tagline">
        Full-stack developer (PERN) with a soft spot for fantasy novels, nature walks, and
        building things that feel like they were made by a person -- because they were.
    </p>
    <div class="cta-row">
        <a class="btn btn-primary" href="/web-dev">See my web work</a>
        <a class="btn btn-secondary" href="/contact">Say hello</a>
    </div>
</section>

<style>
    .hero {
        max-width: 42rem;
        margin: 0 auto var(--space-5);
        text-align: center;
    }

    .eyebrow {
        font-family: var(--font-heading);
        font-size: 1.5rem;
        color: var(--color-secondary);
        margin-bottom: 0;
    }

    .hero h1 {
        margin-top: 0.1em;
    }

    .tagline {
        font-size: 1.15rem;
        color: var(--color-text-muted);
    }

    .cta-row {
        display: flex;
        gap: 1em;
        justify-content: center;
        flex-wrap: wrap;
        margin-top: var(--space-2);
    }
</style>
